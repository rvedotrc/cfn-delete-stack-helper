Installation of cfn-delete-stack-helper
---------------------------------------

Prerequisites:

 * A modern Ruby (at the time of writing, 2.3.x)
 * gem
 * bundler

Installation:

```
  bundle install
  gem build *.gemspec
  gem install *.gem
```

The `cfn-delete-stack-helper` executable should now be available.

